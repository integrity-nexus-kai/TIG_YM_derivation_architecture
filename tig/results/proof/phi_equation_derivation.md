# Phi Equation Derivation

## Purpose

This document derives the next required tensor equation for the exact TIG vacuum-proof program.

The goal is to determine the redshift function:

\[
\Phi(r)
\]

required for exact tensor-level closure of the TIG field equation.

---

# General Metric Ansatz

The exact TIG proof program requires the more general static spherically symmetric metric:

\[
ds^2
=
-e^{2\Phi(r)}B(r)\,dt^2
+
\frac{dr^2}{B(r)}
+
r^2d\Omega^2.
\]

The previously used restriction:

\[
A(r)=B(r)
\]

is insufficient for exact vacuum closure.

---

# Horizon Sector

The TIG horizon structure is encoded in:

\[
B(r)
=
1-\frac{2Mr^2}{r^3+r_c^3}.
\]

The horizon condition remains:

\[
B(r_H)=0.
\]

Therefore:

\[
x^3-x^2+\beta^3=0
\]

remains structurally preserved.

---

# Tensor-Level Obstruction

The previous tensor evaluation showed:

\[
\mathcal H^t_{\ t}
-
\mathcal H^r_{\ r}
\neq0
\]

for the restricted metric sector:

\[
A(r)=B(r).
\]

Therefore exact vacuum closure requires:

\[
\Phi'(r)\neq0.
\]

---

# Required Field Equation

The next exact proof step requires explicit evaluation of:

\[
\mathcal E^t_{\ t}
-
\mathcal E^r_{\ r}
=0.
\]

Under the generalized metric ansatz this produces a differential equation for:

\[
\Phi(r).
\]

---

# Required Calculation Chain

The explicit derivation requires:

1. Christoffel symbols
2. Ricci tensor
3. Ricci scalar
4. Einstein tensor
5. Higher-order correction tensor
6. Residual tensor difference

for the generalized metric sector.

---

# Exact Proof Objective

The exact TIG vacuum proof requires:

\[
\mathcal E_{\mu\nu}=0
\]

component-by-component.

The current unresolved proof step is therefore the explicit derivation and solution of the equation governing:

\[
\Phi(r).
\]

---

# Current Status

The TIG derivation architecture remains:

\[
\boxed{
\text{STRUCTURALLY CONSISTENT,
PROOF-ORIENTED,
NOT YET EXACTLY CLOSED}
}
\]

The current unresolved proof obligation is concentrated in the redshift sector:

\[
\Phi(r).
\]

---

# Next Step

The next required step is explicit symbolic derivation of the differential equation for:

\[
\Phi(r).
\]
